# Environment variables for LLM Assistant Bot

# Claude Code CLI Configuration
# NOTE: Claude Code CLI uses authenticated sessions (Pro/Max subscription) 
# instead of direct API keys. Run 'claude auth' to authenticate.
# CLAUDE_API_KEY is optional and mainly for backward compatibility.
# CLAUDE_API_KEY=your_claude_api_key_here

# Server configuration
HOST=0.0.0.0
PORT=8000
DEBUG=true

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000

# Log level
LOG_LEVEL=INFO

# MCP (Model Context Protocol) Configuration
# MCP servers are configured in .mcp.json and used by Claude Code CLI
# The following environment variables are used by specific MCP servers:

# For gemini-google-search MCP server
# GEMINI_API_KEY=your_gemini_api_key_here

# For brave-search MCP server (if you add it to .mcp.json)
# BRAVE_API_KEY=your_brave_api_key_here

# For github MCP server (if you add it to .mcp.json)
# GITHUB_PERSONAL_ACCESS_TOKEN=your_github_token_here